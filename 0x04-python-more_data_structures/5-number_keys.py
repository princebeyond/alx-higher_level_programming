#!/usr/bin/python3
def number_keys(a_dictionary):
    # add key to the dict list
    # a_dictionary['key'] = 3
    # and return it
    # return a_dictionary.get('key')
    # i was supposed to get the length not to add it in there soo.......
    return len(a_dictionary)
